import PagePlaceholder from './PagePlaceholder'

export default function Calendar() {
  return (
    <PagePlaceholder
      eyebrow="Your month at a glance"
      title="Every deadline, in its place."
      subtitle="See how your assignments fall across the weeks ahead."
      stories={['Stretch goal — month grid built on the same task data as US-10']}
    />
  )
}
